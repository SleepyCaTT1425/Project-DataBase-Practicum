# ตั้งค่าฐานข้อมูล MySQL
DB_HOST=localhost
DB_USER=USER 		# เปลี่ยนเป็น USER ที่ใช้ (MYSQL)
DB_PASSWORD=PASSWORD	# เปลี่ยนเป็น PASSWORD ที่ใช้ (MYSQL)
DB_NAME=kasetfair

#ตั้งค่าสำหรับ Manual Execution
PROMPTPAY_PHONE=08XXXXXXXX
PAYMENT_TIMEOUT_MINUTES=6

