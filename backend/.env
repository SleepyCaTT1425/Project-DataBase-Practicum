# ตั้งค่าฐานข้อมูล MySQL
DB_HOST=localhost
DB_USER=root 		# เปลี่ยนเป็น USER ที่ใช้ (MYSQL)
DB_PASSWORD=Propond14!	# เปลี่ยนเป็น PASSWORD ที่ใช้ (MYSQL)
DB_NAME=kasetfair

# ตั้งค่าระบบชำระเงิน
# --------------------------------
# เบอร์  PROMPTPAY รับเงิน
PROMPTPAY_PHONE=0637961693

# ระยะเวลาที่ระบบจะยกเลิกการจอกหากไม่ส่งสลิป
PAYMENT_TIMEOUT_MINUTES=2